CREATE TABLE zooparkForTrade
(
    zooparkID INT NOT NULL PRIMARY KEY IDENTITY(1,1), -- primary key column
    Name varchar(100) NOT NULL ,
    Adress varchar (100) NOT NULL,
    Nubmer varchar (50) NOT NULL
    -- specify more columns here
);
GO